import type { AppExtension } from "@nocobase/portal-sdk/extensions";

// Dev-only route-surfaces demo removed (incomplete in template). The drawer/dialog
// primitives used by modules live in sibling files and are imported directly.
const routeSurfacesExtension: AppExtension = {
  id: "nocobase-route-surfaces",
};

export default routeSurfacesExtension;
